os:
  - linux
  - osx

language: c
script:
  - make test
  - make clean
  - make CC=g++ CFLAGS=-std=c++98 test
  - make clean
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then make CFLAGS=-g valgrind; fi
addons:
  apt:
    packages:
      - apache2-utils
      - valgrind
